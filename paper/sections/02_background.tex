\section{Background and related work}
\label{sec:background}

% ---------------------------------------------------------------------------
% ~800 words.
%
% Cite Bennett (1948), Sripad & Snyder (1977), Widrow & Kollar (2008)
% OURSELVES, as the foundation. Objections v2.1 Z.2 is explicit: the "this is
% Bennett with a gyroscope attached" objection must be pre-empted by citing
% Bennett first, not conceded later.
%
% Then IEEE-952 Q and why it is the wrong architecture for a rate register.
% Then Table 1.
%
% FRAMING FOR TABLE 1 -- rewritten 30 July 2026 after the primary sources were
% read directly. The earlier framing, from GMWM-to-Kalman-Q v1.2 Z.4, was
%
%   "The surveyed pipelines DO NOT MODEL register quantisation AT ALL."
%
% That is TOO STRONG as a blanket claim and a referee who knows the tools will
% catch it. The Allan-variance literature these tools derive from fits the
% five-coefficient model Q/N/B/K/R, and GMWM implementations offer a
% quantisation process.
%
% The sustainable claim is SHARPER, not weaker:
%
%   Where a quantisation coefficient exists at all it is the IEEE-952 Q at
%   -1 slope, which is an ANGLE-INCREMENT construct. A rate register never
%   produces that slope, so the coefficient is fitted in the wrong place and
%   the register's contribution is absorbed by the white-noise process.
%   No tool links a quantisation term to Delta, FSR or rho.
%
% That version survives the informed referee, and it is the argument the data
% actually supports.
% ---------------------------------------------------------------------------

\begin{table*}[t]
\centering
\caption{What stochastic calibration pipelines model. Rows 1--3 verified
against the primary source, 30 July 2026; rows 4--6 \todo{still to verify}.
No pipeline exposes $\rhod$ or $\mu$, and none links a quantisation term to
$\Dlsb$, full-scale range or output word length.}
\label{tab:toolchains}
\small
\begin{tabular}{p{0.16\textwidth}p{0.20\textwidth}p{0.24\textwidth}p{0.16\textwidth}l}
\toprule
Pipeline & Discrete-time white noise & Quantisation coefficient
         & Reports & Source \\
\midrule
Kalibr
  & $\sigma_d = \sigma_c/\sqrt{\Delta t}$, stated as a \emph{condition}
    requiring ideal decimation
  & none. Model is
    $\tilde\omega = \omega + b + n$
  & $\sigma_g$, $\sigma_{b_g}$
  & \cite{kalibrwiki} \\[4pt]
\texttt{allan\_variance\_ros}
  & inherited: emits Kalibr \texttt{imu.yaml} with \texttt{update\_rate}
  & none
  & ARW, bias instability, rate random walk
  & \cite{allanvarianceros} \\[4pt]
\texttt{imu\_utils}
  & implicit
  & none reported \todo{internal fit may include $Q$ --- check the source}
  & \texttt{gyr\_n}, \texttt{gyr\_w} only
  & \cite{imuutils} \\[4pt]
GMWM (\texttt{simts}/\texttt{wv})
  & per-configuration fit
  & IEEE-952 $Q$, $-1$ slope only
  & \todo{}
  & \todo{cite} \\[4pt]
MATLAB Sensor Fusion
  & \todo{verify}
  & \todo{verify}
  & \todo{}
  & \todo{cite} \\[4pt]
NaveGo
  & \todo{verify}
  & \todo{verify}
  & \todo{}
  & \todo{cite} \\
\bottomrule
\end{tabular}
\end{table*}

% ---------------------------------------------------------------------------
% VERBATIM QUOTATIONS, verified against the source 30 July 2026. These are the
% most load-bearing citations in the paper: the toolchain conceding the
% condition in its own documentation is far better evidence than anything we
% could infer from its source code.
%
% On the transfer rule (Kalibr wiki, "Additive White Noise"):
%
%   "Note: This assumes that the noise was filtered with an ideal low-pass
%    filter that filters noise above f = 1/(2 dt) (in other words, an ideal
%    decimation stage). This may or may not be the case, depending on your
%    sensor settings. If you simply "subsample" your gyro or accel, you are
%    not allowed to scale your "white noise density" in that way."
%
% On the model's own adequacy (same page, "in Practice"):
%
%   "So clearly, the model is optimistic. ... From our experience, for
%    lowest-cost sensors, increasing the noise model parameters by a factor of
%    10x or more may be necessary."
%
% TWO CORRECTIONS TO EARLIER INTERNAL DRAFTS:
%
%  1. The inflation factor is "10x or more", NOT "five-to-ten times".
%  2. This page is NOT Rehder et al. (2016). It is the project wiki, last
%     revised by Patrick Geneva in March 2023. Cite it as documentation with
%     an access date; cite the paper separately if the paper is what is meant.
%
% AND ONE POINT WORTH A SENTENCE OF ITS OWN: Kalibr's reference [1] for the
% Allan-variance method is IEEE Std 952-1997 -- the FOG standard. That is this
% paper's argument in a single line. The method is imported from an
% angle-increment instrument and applied to a rate register without the
% architectural distinction ever being made.
% ---------------------------------------------------------------------------

\todo{Write.}
